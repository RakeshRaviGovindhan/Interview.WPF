<UserControl x:Class="InterviewWPF.Views.AccountView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InterviewWPF.Views" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Background="WhiteSmoke" Height="30" VerticalAlignment="Top">
            <Label Content="{Binding WelcomePage}" VerticalAlignment="Center" FontWeight="DemiBold"  HorizontalAlignment="Center"/>
        </StackPanel>

        <Button Height="40" Width="200" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="-1 10"
                Command="{Binding NavigateHomeCommand}" Background="Teal" BorderBrush="{x:Null}">
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Foreground="White" VerticalAlignment="Center"  Kind="ArrowLeft" Height="20" Width="20"/>
                <Label Content="Go to Home page" Margin="10 0 15 0" Foreground="White"/>
            </StackPanel>
        </Button>
    </Grid>
</UserControl>
